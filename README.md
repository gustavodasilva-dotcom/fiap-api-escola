# fiap-api-escola

Web API desenvolvida em .NET Core 8.

Para executar, é possível fazê-lo através do Visual Studio ou através da CLI do .NET.

Na pasta raiz, basta executar:

```
dotnet run --project src/Api/Api.csproj
```